# EIGG Investigate — single-host reverse proxy.
# Routes /api/v1 + /health to the backend; everything else (pages, NextAuth, admin
# proxy) to the frontend. HTTP on :80 by default. For TLS, uncomment the 443 block,
# mount ./certs in docker-compose.yml, and point the cert paths below.

upstream eigg_backend  { server backend:8000; }
upstream eigg_frontend { server frontend:3000; }

server {
    listen 80;
    server_name _;
    client_max_body_size 25m;

    location /api/v1/ {
        proxy_pass http://eigg_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    location /health { proxy_pass http://eigg_backend; }
    location / {
        proxy_pass http://eigg_frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# --- HTTPS (optional) ---------------------------------------------------------
# server {
#     listen 443 ssl;
#     http2 on;
#     server_name eigg.your-domain.internal;
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     client_max_body_size 25m;
#     location /api/v1/ { proxy_pass http://eigg_backend; proxy_set_header Host $host; proxy_set_header X-Forwarded-Proto $scheme; }
#     location /health  { proxy_pass http://eigg_backend; }
#     location /        { proxy_pass http://eigg_frontend; proxy_set_header Host $host; proxy_set_header X-Forwarded-Proto $scheme; }
# }
